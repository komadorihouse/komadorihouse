<div class="header">
  <%= render "shared/frist-header"%>
</div>

<div class="body">
<%# ここから左辺の目次欄 %>
  <%= render "shared/works_index"%>
  <%# ここからメインコンテンツ表示域 %>
<div class="works-contents">
  <div class="works-info">
      Overview
  </div>
    <div class="contents-body">
      <ul>
        <li>
          <span class="small">creater -</span> <%= @creater %>
        </li>
        <li>
          <span class="small">created yaer -</span> <%= @item.created_year %>
        </li>
        <li class="description">
          <%= simple_format(@item.description) %>
        </li>
        <li>
          <% if @item.youtube.present? %>
            <iframe width="100%" height="506.25" src="https://www.youtube.com/embed/<%= @item.youtube %>" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
          <% end %>
            <%= image_tag ("http://drive.google.com/uc?export=view&id=#{@item.image}"),id:"work-image" %>
        </li>
      </ul>
    </div>
  </div>
</div>
<div class="footer-area">
  <%= render "shared/footer"%>
</div>